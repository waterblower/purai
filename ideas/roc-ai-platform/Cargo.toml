[package]
name = "roc-ai-platform"
version = "0.1.0"
edition = "2021"
license = "UPL-1.0"
description = "A small Rust-hosted Roc platform for standard and file I/O"

[lib]
name = "host"
crate-type = ["staticlib", "rlib"]

[profile.release]
panic = "abort"

[dependencies]

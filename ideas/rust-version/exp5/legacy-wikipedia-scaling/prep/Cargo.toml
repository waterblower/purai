[package]
name = "exp5-prep"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
parquet = { version = "56", default-features = false, features = ["snap"] }
serde_json = "1.0"

[profile.dev]
opt-level = 2
